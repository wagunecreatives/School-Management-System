# TODO

- [ ] Add status badge rendering to invoices table in `src/routes/_authenticated/accountant/fees.tsx`
  - [ ] Keep `<TableHead>Status</TableHead>`
  - [ ] Replace the current status cell (`className="capitalize"` + `{i.status}`) with conditional spans for Deleted / Cancelled / Active.

